// Scalar Values
// const lets scalar values be immutable. you cannot change the value of answer / greeting
const answer = 40;
const greeting = 'Sandeep';



// Arrays and Objects

const numbers = [1, 2, 3];
const person = {
name: 'Sandeep',
age: 29
};

// const lets the variable points to the same arrays & objects but the values can be changed - find example online
